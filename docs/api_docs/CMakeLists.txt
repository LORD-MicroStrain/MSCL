# Although there isn't much in this file, it keeps the docs projects separate in the build directory

# Set the docs title for the Project.txt configure_file call
set(MSCL_DOCS_TITLE "MSCL Documentation")
mscl_configure_docs_target("MSCL_API_Docs")
