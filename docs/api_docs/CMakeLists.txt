# Although there isn't much in this file, it keeps the docs projects separate in the build directory
mscl_configure_docs_target(
    ROOT_CONFIGURE_FILES_DIR "${MSCL_DOCS_ROOT_CONFIGURE_FILES_DIR}"
    API_DOCS
)
