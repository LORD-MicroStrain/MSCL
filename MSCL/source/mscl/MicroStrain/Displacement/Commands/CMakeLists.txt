set(MSCL_MICROSTRAIN_DISPLACEMENT_COMMANDS_FILES
    "${CMAKE_CURRENT_LIST_DIR}/DeviceTime.cpp"
    "${CMAKE_CURRENT_LIST_DIR}/DeviceTime.h"
    "${CMAKE_CURRENT_LIST_DIR}/DisplacementOutputDataRate.cpp"
    "${CMAKE_CURRENT_LIST_DIR}/DisplacementOutputDataRate.h"
    "${CMAKE_CURRENT_LIST_DIR}/GetAnalogToDisplacementCals.cpp"
    "${CMAKE_CURRENT_LIST_DIR}/GetAnalogToDisplacementCals.h"
)

# Filter and add the source files to the target
set(MSCL_MICROSTRAIN_DISPLACEMENT_COMMANDS_SOURCES ${MSCL_MICROSTRAIN_DISPLACEMENT_COMMANDS_FILES})
list(FILTER MSCL_MICROSTRAIN_DISPLACEMENT_COMMANDS_SOURCES INCLUDE REGEX "^.*\\.(c|cpp)$")
target_sources("${MSCL_LIBRARY}" PRIVATE "${MSCL_MICROSTRAIN_DISPLACEMENT_COMMANDS_SOURCES}")

# Filter and add the header files to the target using file sets
set(MSCL_MICROSTRAIN_DISPLACEMENT_COMMANDS_HEADERS ${MSCL_MICROSTRAIN_DISPLACEMENT_COMMANDS_FILES})
list(FILTER MSCL_MICROSTRAIN_DISPLACEMENT_COMMANDS_HEADERS INCLUDE REGEX "^.*\\.(h|hpp)$")
target_sources("${MSCL_LIBRARY}" PUBLIC
    FILE_SET "${MSCL_HEADER_FILE_SET}"
    FILES "${MSCL_MICROSTRAIN_DISPLACEMENT_COMMANDS_HEADERS}"
)
